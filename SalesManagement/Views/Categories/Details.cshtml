@using SalesManagement.Models.Entities
@model Category

@{
    ViewData["Title"] = "Category Details";
}

<div class="container py-4">

    <!-- Page Header -->
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center mb-4">
        <h1 class="fw-bold mb-3 mb-md-0">Category Details</h1>
        <div class="d-flex gap-2">
            <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary rounded-pill px-4">
                <i class="bi bi-pencil-square me-2"></i> Edit
            </a>
            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">
                <i class="bi bi-arrow-left-circle me-2"></i> Back to List
            </a>
        </div>
    </div>

    <!-- Category Details Card -->
    <div class="card shadow-sm rounded-4 mb-4">
        <div class="card-body p-4 p-md-5">
            <h4 class="fw-semibold mb-3">Category Info</h4>
            <hr />

            <dl class="row mb-0">
                <dt class="col-sm-4 fw-semibold">Title</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Title)</dd>
            </dl>
        </div>
    </div>

    <!-- Product Types Table Card -->
    <div class="card shadow-sm rounded-4">
        <div class="card-body p-4 p-md-5">
            <h4 class="fw-semibold mb-3">Product Types</h4>
            <hr />

            <div class="table-responsive">
                <table class="table table-bordered table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Title</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.ProductTypes)
                        {
                            <tr>
                                <td>@item.Title</td>
                                <td class="text-center">
                                    <a asp-controller="ProductTypes"
                                       asp-action="Details"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-outline-primary rounded-pill">
                                        <i class="bi bi-info-circle"></i> Details
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

        </div>
    </div>

</div>
